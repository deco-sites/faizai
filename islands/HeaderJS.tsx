import Component from "$store/components/header/HeaderJS.tsx";
import type { Props } from "$store/components/header/HeaderJS.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
